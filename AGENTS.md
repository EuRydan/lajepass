# Regras do Projeto (laje pass)

## Regra de Versionamento (Git)
- **Sempre faça `git push` no fim de cada alteração.**
- Após realizar qualquer modificação solicitada pelo usuário e verificar que está correta, faça o commit correspondente e o `git push` para o repositório remoto.
